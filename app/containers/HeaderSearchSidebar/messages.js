/*
 * HeaderSearchSidebar Messages
 *
 * This contains all the text for the HeaderSearchSidebar container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HeaderSearchSidebar';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HeaderSearchSidebar container!',
  },
});
